import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        borderColor: 'black',
        borderWidth: 1,
        width: 200,
        padding: 10,

    },
    label: {
        color: 'black',
        fontSize: 12,
        textAlign: 'center'
    },
});

export default styles;