import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        borderColor: 'black',
        borderWidth: 1,
        width: 220,
        padding: 10,
        textAlign: 'center'

    },
    label: {
        color: 'black',
        fontSize: 12,
        textAlign: 'center'
    },
});

export default styles;